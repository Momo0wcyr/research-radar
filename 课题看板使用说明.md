# 课题雷达使用说明

适用文件：`Research Radar.html`

## 1. 工具用途

课题雷达是一个面向高校教师、课题组负责人和研究团队管理者的本地课题总览工具。它用于集中记录：

- 课题名称与编号；
- 负责学生和协作学生；
- 当前关键节点；
- 导师人工判断的线性进度；
- 已完成的关键结果或里程碑；
- 当前问题；
- 拟解决方案；
- 下一步行动；
- 下次汇报时间；
- 最后更新时间。

该版本是一个独立的HTML文件，不需要安装软件、不需要启动服务器，也不需要联网。

## 2. 运行要求

推荐使用以下浏览器打开：

- Microsoft Edge；
- Google Chrome；
- 其他支持现代网页功能的浏览器。

Windows用户直接双击HTML文件即可。如果系统询问打开方式，选择Edge或Chrome，并可勾选“始终使用此应用打开HTML文件”。

## 3. 首次使用

### 3.1 固定文件位置

建议先将HTML文件放在一个长期不变的位置。

开始录入数据后，不建议随意移动或重命名HTML文件。部分浏览器可能把不同文件路径视为不同的本地页面，从而导致原数据暂时看不到。

如果确实需要移动、重命名或复制到另一台电脑，请先导出JSON备份。

### 3.2 打开看板

双击HTML文件。页面右上角显示：

- “导出桌面总览”；
- “新建课题”。

标题区域右侧会根据电脑系统时间自动显示星期、日期、年份和月份。

## 4. 新建课题

点击右上角“新建课题”，填写课题信息。

### 4.1 建议必填字段

- **课题名称**：使用稳定、清晰的研究主线名称；
- **课题编号**：每个课题必须唯一；
- **负责学生**：填写主要执行或汇报学生；
- **当前节点**：例如“课题调研”“分子设计”“首批合成”“活性验证”“机制研究”“动物实验”“论文整理”。

### 4.2 课题编号建议

推荐采用“研究类型—核心对象—序号”的结构

编号不建议包含学生姓名和年份，以免更换负责人或跨年度延续时需要重新编号。

AI更新包通过课题编号匹配项目，因此不同课题不能使用相同编号。

## 5. 状态管理

可选择以下状态：

- **待设定**：导师尚未判断；
- **正常**：按预期推进；
- **关注**：存在风险，需要持续观察或补充证据；
- **困难**：当前存在关键问题，无法继续正常推进；
- **已完成**：课题或既定阶段已经完成。


## 6. 线性进度管理

进度范围为0–100%，可通过滑块或数字框填写。

### 6.1 基本原则

- 进度完全由导师判断；
- 不根据任务数量自动计算；
- 不同课题可以采用不同的判断标准；
- AI只能提出建议区间，不能自动修改最终进度。

建议同时填写“导师的进度判断依据”，例如：

```text
核心母核和首轮SAR已经完成，但细胞机制与DMPK尚未验证，因此当前判断为45%。
```

这样可以避免几个月后无法回忆进度调整依据。

如果暂时无法判断，可清空数字框，使页面显示“待设定”。

## 7. 关键内容的填写方式

### 7.1 课题目标

填写课题希望最终解决的问题，不要写成当周任务。例如：

```text
获得兼具细胞降解活性和可接受药代性质的CDK9靶向降解剂。
```

### 7.2 关键结果或里程碑

只填写已经完成并有证据支持的工作，例如：

- 已合成并完成结构确证的化合物；
- 已取得的生化、细胞或动物实验结果；
- 已确认的构效关系；
- 已完成的机制验证；
- 已投稿、返修或接受的论文。

“已送测”“拟开展”和“预计有效”不能写成已经完成的结果。

### 7.3 当前问题

填写影响下一步推进的核心问题，例如：

- 活性结果无法重复；
- 直接结合证据不足；
- 细胞活性与生化活性不一致；
- 溶解度、稳定性或暴露不足；
- 对照实验缺失；
- 实验体系尚未建立。

### 7.4 解决方案

填写导师确认后的解决路径。如果证据不足或尚未决策，可以留空，不必强行填写。

### 7.5 下一步行动

尽量写成明确、可检查的动作，例如：

```text
完成6个代谢软点封闭类似物的合成，并比较DC50、Dmax和微粒体稳定性。
```

避免只写“继续推进”“进一步研究”等无法检查的表述。

## 8. 浏览、筛选和排序

看板支持：

- 按状态筛选；
- 按课题名称、编号、学生或节点搜索；
- 按优先级排序；
- 按下次汇报时间排序；
- 按进度排序。

点击一条课题记录，可以展开查看目标、问题、解决方案和下一步行动。

## 9. 数据保存机制

### 9.1 自动保存

每次点击“保存课题”后，数据会自动保存在当前浏览器中。日常修改不需要每次都导出JSON。

### 9.2 数据保存在哪里

数据不写入HTML文件本身，而是保存在：

> 当前电脑 + 当前浏览器 + 当前浏览器配置 + 当前HTML文件访问位置

因此，以下操作可能导致原数据看不到：

- 清除浏览器缓存或网站数据；
- 使用浏览器隐私/无痕模式；
- 更换浏览器；
- 更换Windows账户；
- 移动或重命名HTML文件；
- 重装系统；
- 更换电脑。

数据看不到不一定代表HTML损坏，通常是浏览器读取了另一个本地存储位置。

## 10. JSON备份

### 10.1 何时备份

不需要每次修改都备份。建议在以下时间导出：

- 每周或每两周一次；
- 批量更新多个课题后；
- 导入AI更新包之前；
- 移动或重命名HTML文件之前；
- 清理浏览器数据之前；
- 换电脑或重装系统之前；
- 向他人移交课题数据时。

### 10.2 如何导出

点击页面下方“导出数据”。浏览器会下载一个JSON文件，例如：

```text
课题雷达备份-2026-08-10.json
```

建议至少保留最近3个日期版本，不要每次覆盖同一个文件。

### 10.3 如何恢复

1. 打开HTML看板；
2. 点击“导入数据或更新包”；
3. 选择之前导出的JSON备份；
4. 确认替换当前数据。

完整备份导入会替换当前看板中的全部课题。导入前应先确认当前看板是否还有未备份的新数据。

## 11. 根据学生总结生成AI更新包

推荐工作流程：

1. 从看板导出最新JSON备份；
2. 准备学生的周报、月报、学期PPT、Word总结或文字记录；
3. 将JSON备份和学生总结一起提供给Codex；
4. 要求Codex区分已有事实、合理解释和后续计划；
5. Codex生成与课题编号对应的JSON更新包；
6. 在看板中点击“导入数据或更新包”；
7. 检查变更预览；
8. 确认后应用文字更新；
9. 由导师手动修改进度和最终状态；
10. 完成后再次导出JSON备份。

可使用以下提示语：

```text
请根据学生总结和课题雷达备份生成AI更新包。
请按课题编号匹配，区分已有事实、合理推断和后续计划。
不得把拟开展实验写成已完成结果。
进度只提供建议区间，不得自动修改最终百分比。
存在课题匹配歧义时请单独列出，不要自行归类。
```

## 11. 导出桌面总览

点击右上角“导出桌面总览”，可生成1920×1080 PNG图片。

该图片可以：

- 设为Windows桌面背景；
- 放入组会PPT；
- 用于阶段性工作汇总；
- 发给课题组成员查看。

当前壁纸最多展示排序后的前8个课题。需要重点展示某些课题时，可以先通过状态和进度进行整理，或暂时调整课题顺序。

## 12. 分享给他人使用

### 12.1 仅分享空白模板

只发送HTML文件。对方打开后建立自己的课题数据，不会看到您的课题。

### 12.2 分享模板和课题数据

同时发送：

- HTML模板；
- 导出的JSON备份。

对方打开HTML后导入JSON即可获得相同课题记录。

### 12.3 数据是否会实时同步

不会。每个人的浏览器保存独立副本。A电脑上的修改不会自动出现在B电脑上。


## 13. 常见问题

### 13.1 每日日期不显示或不正确

- 检查Windows系统日期和时区；
- 关闭页面后重新打开；
- 按`Ctrl + F5`强制刷新；
- 使用修复后的HTML版本，而不是旧模板。

日期读取电脑系统时间，无法通过看板单独修改。

### 13.2 双击HTML后显示代码文本

右键HTML文件，选择：

```text
打开方式 → Microsoft Edge 或 Google Chrome
```

不要使用记事本或Word打开。

### 13.3 原来的课题突然不见了

依次检查：

1. 是否使用了原来的浏览器；
2. 是否使用了相同的Windows账户；
3. HTML文件是否被移动或重命名；
4. 是否打开了另一个同名模板；
5. 是否清除了浏览器数据；
6. 是否有最近的JSON备份。

如有备份，直接导入即可恢复。

### 13.4 JSON无法导入

确认文件是：

- 由本看板导出的完整备份；或
- 符合课题雷达格式的AI更新包。

不要直接把Word、PPT、Excel或普通文本文件改名为`.json`。

### 13.5 更新包提示课题未匹配

通常是更新包中的课题编号与看板编号不一致。检查：

- 大小写；
- 连字符；
- 空格；
- 编号是否被修改；
- 是否存在重复编号。

### 13.6 是否可以在手机上使用

页面支持窄屏显示，但手机浏览器对本地HTML和文件下载的支持不完全一致。建议主要在Windows电脑上维护，手机仅用于临时查看。

## 14. 推荐的日常工作节奏

### 每次学生汇报后

- 更新关键结果；
- 更新当前问题；
- 确认解决方案；
- 更新下一步行动；
- 设置下次汇报时间；
- 由导师调整状态和进度。

### 每周

- 查看“困难”和“关注”课题；
- 检查7天内需要汇报的课题；
- 导出一份JSON备份。

### 每月或每学期

- 检查课题编号和负责人；
- 归档已完成课题；
- 判断平台课题是否需要拆分子课题；
- 导出桌面总览用于工作汇报；
- 保存带日期的阶段性JSON备份。

## 15. 隐私和安全

- HTML模板本身不会主动上传数据；
- 数据默认保存在本地浏览器；
- AI更新需要您主动将总结和备份提供给AI；
- 分享HTML前，确认发送的是空白模板还是包含数据的JSON；
- 涉及未公开化合物结构、专利点或敏感实验数据时，应按照课题组和单位的数据管理要求处理。

## 16. 建议的文件目录

```text
课题管理\
├─ 课题看板.html
├─ README-课题看板使用说明.md
└─ 备份\
   ├─ 课题雷达备份-2026-08-10.json
   ├─ 课题雷达备份-2026-08-17.json
   └─ 课题雷达备份-2026-08-24.json
```

将HTML、说明文件和JSON备份分开保存，既便于使用，也可避免误删。
